//const {series}=obj.GoodreadsResponse
//series is goodreadsresponse.series
//titel is series.title._cdata)
//descriptino is series.description._cdata
//series.series_works_count._text (note text)

// console.log(obj.GoodreadsResponse.series)
// first work in a series:
// obj.series.series_works.series_work[0])
// title of work
// obj.series.series_works.series_work[0].work.best_book.title._text)
//author of book
// console.log(obj.series.series_works.series_work[0].work.best_book.author.name._text)
//book image!
// console.log(obj.series.series_works.series_work[0].work.best_book.image_url._cdata)

// eventually
// list.book   —   Get the listopia lists for a given book.
